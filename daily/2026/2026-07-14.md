# 2026-07-14 工作日志

## 扫描范围

- 变更文件数：4（Claude Code 当日会话记录）；知识库日内变更为 3 个 Markdown。
- Token 数：约 150k（可从本次 Claude Code 会话记录的 API usage 读取；OpenClaw 原始 JSONL 未单独可得精确 token）。
- 变更文件列表：4 个 Claude Code session JSONL（含 yq66、WorkBuddy、Skill Workshop、Langfuse 工作）；完整清单见本段说明。
- 完整变更文件清单：已嵌入（4 项）：`b6d68e45-94ee-49f7-8236-0243a26d5fbc.jsonl`、`4fdb2e86-da78-4bff-acda-91397318a2c6.jsonl`、`4a6b5496-b921-4477-8723-802da9a10285.jsonl`、`4491d7cc-9885-462b-9bc7-b2e531baf476.jsonl`。
- OpenClaw 记忆扫描：3 files，2026-07-11 至 2026-07-13，约 6.5k chars；无因预算跳过文件。
- OpenClaw 原始会话 JSONL：bounded extraction: 1 file，约 31k 增量 bytes（offset 239941→271049；该文件当前总约 271k bytes），读取当前正常会话的受控结构化摘要；trajectory 与 reset 文件排除。
- 不可用来源：Obsidian Copilot 当日会话目录未发现可用输入；预压缩检查点目录不存在。

## 今日目标

- 维护多中转站的安全路由控制面，并保持 LiteLLM 按需求启停。

## 今日完成

- 核验新增 `yq66` 已进入 relay roster；其 Anthropic 与 OpenAI-compatible 能力已登记，Responses 能力仍待确认。
- 确认现有客户端分配未因新增 relay 改变：Claude Code、OpenClaw、Codex 仍使用 `gsykj`；LiteLLM 保持停用。
- 刷新并核验 `model-route-switcher`：支持多 relay 注册、修改、带引用检查的删除、单客户端分配/回滚，以及默认与当前会话路由的证据化查询。

## 今日完成（增量）

- `yq66` 已通过认证连通性验证：Anthropic 与 OpenAI-compatible 模型端点均返回 HTTP 200，发现 10 个模型；未改变现有 agent assignments。
- WorkBuddy CLI 升级至全局包 `@workbuddy/cli@1.0.1`，已恢复 help/命令入口；其专用环境投影复用 Claude Code 当前 `gsykj` Anthropic relay，保留按需切换边界。
- Skill Workshop 的显式 apply 流程已配置为自动策略并通过无副作用测试；OpenClaw Langfuse extension 已增加可配置的 sanitized/metadata/off 上下文捕获模式并重启验证本地调用成功。

## 关键结论

- 配置、运行会话及 LiteLLM 网关是不同证据层级；网关日志或默认配置不能单独归因一个运行会话的实际路由。
- `yq66` 已验证可用但仍仅是候选 relay，不代表任何客户端已经切换过去；Codex 切换前还须验证 Responses wire API。
- Langfuse 插件的本地运行验证成功，但 Langfuse 服务端 trace-list API 受 ClickHouse schema 异常阻断，trace 内容尚未完成服务端验收。
- LiteLLM 容器当前已停止；在没有已验证需求时不应启动它。

## 问题与风险

- `yq66` 的 Codex 所需 Responses wire API 尚未确认，切换 Codex 前需要先验证。
- WorkBuddy CLI 启动问题已通过升级解决；仍需在后续实际业务调用时验证其是否真正消费该模型 relay projection。
- `approval-flow-test` 是流程验证用临时 skill，如不再需要应按显式请求清理。

## 产出物

- 路由流程：`/Users/mjhorse/.openclaw/workspace/skills/model-route-switcher/SKILL.md`
- Relay roster：`/Users/mjhorse/.config/ai-relays/relays.json`
- 客户端分配：`/Users/mjhorse/.config/ai-relays/assignments.json`
- WorkBuddy 修复计划：`/Users/mjhorse/.claude/plans/floofy-tinkering-flask.md`
- Langfuse extension：`/Users/mjhorse/.openclaw/extensions/openclaw-langfuse-session/index.js`

## 知识链接

<!-- notehelper-links -->
- [[projects/litellm-langfuse/overview#当前路由与网关状态|LiteLLM 与多 Relay 当前状态]]
<!-- notehelper-links -->

## 明日计划

- 如需切换客户端到 `yq66`，先按协议能力进行只读验证；Codex 需额外验证 Responses 支持。
- 如继续 WorkBuddy，先完成 CLI 兼容性修复和无密钥泄露的投影验证。

## 待沉淀知识

- 将多 relay 分配和回滚实操记录扩展为单独的操作手册（若后续发生实际切换）。
