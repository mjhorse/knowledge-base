# 2026-07-06 工作日志

## 今日目标

- 自动汇总 02:00 前后可确认的本地工作进展，重点记录 Claude Code 会话中的 durable facts。

## 今日完成

- 继续处理 `/Users/mjhorse/workforce-planning-web` 的未提交改动：
  - 按 `/simplify` 流程做代码复用、质量、效率三类并行审查。
  - 修复 `trackStateMutation` 的 `traceType` 记录错误。
  - 抽出 `withMutationTrace` 减少 traced setter 重复包装。
  - 抽出 `applyDemandBudgetStatus`，统一需求编辑和提交里的预算状态联动逻辑。
  - 复用 `budgetPoolSourceOptions`，避免重复定义预算来源选项。
  - 抽出 `downloadCsvLines`，统一 CSV 下载并在下载后 `URL.revokeObjectURL`，避免 object URL 泄漏。
  - 优化 `WorkloadFields` 内估算成本计算，避免输入热路径里走完整预算池匹配逻辑。
  - 运行 `cd /Users/mjhorse/workforce-planning-web && npm exec vite -- build`，构建通过，仅有 Vite chunk size 警告。
- 继续排查 LiteLLM + Langfuse 本地观测链路：
  - 轮换本地 `LITELLM_MASTER_KEY`，更新 `/Users/mjhorse/ai-observability/litellm/.env`，未暴露密钥值。
  - 重启 Docker 容器 `litellm-gateway`，确认 readiness healthy。
  - 定位上游 `403 / error code: 1010` 为 relay / Cloudflare 风控按请求特征拦截；同请求加入浏览器风格 `User-Agent: Mozilla/5.0` 与 `Accept: application/json` 后可通过 `/v1/models`。
  - 更新 `/Users/mjhorse/ai-observability/litellm/config.yaml`，为 OpenAI-compatible 上游请求增加 `extra_headers`。
  - 将 `codex-default` 映射调整为可用的 `openai/gpt-5.5`；`gpt-5.5` 与 `codex-default` 通过 LiteLLM 请求成功。
  - 确认 `claude-sonnet-4-6` 失败原因已从 WAF 拦截变为上游账号/通道模型权限限制。
  - 用 curl 验证 LiteLLM 请求 `codex-default` HTTP 200，并确认 Langfuse/ClickHouse 中已有成功 trace/observation、token、cost 与可计算 latency。
  - 尝试 Codex CLI 通过本地 LiteLLM：请求已打到本地 `/v1/responses`，但 LiteLLM Responses API 在 Codex 会话存储/embedding/auth 路径上返回 `No connected db`，需要后续给 LiteLLM 接数据库或调整 Codex 接入方式。

## 关键结论

- `workforce-planning-web` 当前本地代码构建已通过，剩余风险主要是未做完整手工回归、未提交改动。
- LiteLLM 上游 `403 / 1010` 不是 LiteLLM master key、容器启动或本地网络问题，而是上游 WAF/风控对请求特征敏感；增加浏览器风格请求头可绕过 `/v1/models` 层面的拦截。
- `gpt-5.5` 可用；`codex-default` 已临时映射到 `gpt-5.5` 并可用。
- `claude-sonnet-4-6` 当前受上游账号/通道模型权限限制，需 relay 侧开通或更换支持 Claude 的上游 key/账号。
- 上游 relay key 本机没有新的备用 key，不能凭空轮换；需要在 relay 管理后台生成新 key 后再替换。

## 问题与风险

- `/Users/mjhorse/workforce-planning-web` 仍有未提交改动：`src/main.jsx`、`src/styles.css`、`src/tracing.js`。
- `workforce-planning-web` 尚需手测关键流程：需求提交/编辑、CSV 导入导出、特性管理、集成点管理、预算分析。
- LiteLLM Responses API 支持 curl，但 Codex CLI 触发的会话/embedding/auth 路径出现 `No connected db`。
- Langfuse public API 查询曾返回 502，但 ClickHouse 侧可查到成功写入的 trace/observation。
- OpenClaw workspace 当日 memory 与长期 `MEMORY.md` 不存在，未纳入本次总结。

## 产出物

- `/Users/mjhorse/workforce-planning-web/src/main.jsx`
- `/Users/mjhorse/workforce-planning-web/src/styles.css`
- `/Users/mjhorse/workforce-planning-web/src/tracing.js`
- `/Users/mjhorse/ai-observability/litellm/.env`（已更新密钥，内容不记录）
- `/Users/mjhorse/ai-observability/litellm/config.yaml`
- `/Users/mjhorse/knowledge-base/projects/litellm-langfuse/verification.md`
- `/Users/mjhorse/knowledge-base/projects/litellm-langfuse/troubleshooting.md`
- `/Users/mjhorse/knowledge-base/troubleshooting/litellm-openai-sdk-headers-blocked.md`

## 知识链接

<!-- notehelper-links -->
- [[projects/litellm-langfuse/verification|LiteLLM + Langfuse 验证记录]]
- [[projects/litellm-langfuse/troubleshooting|LiteLLM + Langfuse 项目排障记录]]
- [[troubleshooting/litellm-openai-sdk-headers-blocked|问题：LiteLLM/OpenAI SDK 风格请求被上游拦截]]
<!-- notehelper-links -->

## 明日计划

- 对 `workforce-planning-web` 做关键流程手工回归，确认后整理 diff 并提交 commit。
- 在 relay 管理后台生成新的上游 key，再替换本机相关配置并重启验证。
- 若需要 Codex CLI 走本地 LiteLLM，继续处理 LiteLLM DB/Responses API 会话路径问题。

## 待沉淀知识

- Codex CLI 新版本只支持 Responses wire API 的接入限制与 LiteLLM 本地网关配置经验。
- 上游 relay/WAF 对 SDK 请求特征敏感时，通过 LiteLLM `extra_headers` 补浏览器风格请求头的排障方法。
